# DEEPEN-LOG

- 2026-09-23 · Port nghiệp vụ **đóng thùng (Map_Can)** từ 2023.B.EQR: thêm entity `Carton` (CanNo, BoxCount) + liên kết `Box.CartonId`, service `PackCartonAsync` (đóng N hộp vào 1 thùng, chặn hộp đã thuộc thùng khác, bỏ qua khi trùng), `CartonController` + 3 view (Index/Create/Detail) + nav "Thùng tem" + seed thùng mẫu. Build Release 0 error; smoke test /Carton, /Carton/Create, /Carton/Detail, POST đóng thùng đều OK.
- 2026-09-23 · Port nghiệp vụ **nhập kho thành phẩm (InvF_InventoryInFG)** từ 2023.B.EQR: thêm entity `InventoryInFG` (InvInNo, Status PENDING/APPROVE, Remark, ApprovedAt/By) + `InventoryInFGDtl` (ProductId, Qty, ProductionDate), service `CreateInventoryInFGAsync` (mọi dòng phải có SP hợp lệ + SL>0, chặn trùng mã phiếu) và `ApproveInventoryInFGAsync` (chỉ duyệt phiếu PENDING), `InventoryInController` + 3 view (Index/Create/Detail, form nhiều dòng động) + nav "Nhập kho TP" + seed phiếu mẫu PNK-SEED-001. Build Release 0 error; smoke test /InventoryIn, /InventoryIn/Create, /InventoryIn/Detail, POST tạo phiếu (PENDING), POST duyệt (APPROVE), chặn trùng mã phiếu đều OK.
- 2026-09-23 · Port nghiệp vụ **phiếu tem rách/vỡ (InvF_BrokenStamp)** từ 2023.B.EQR: thêm entity `BrokenStamp` (BsNo, Quantity, Status) + `BrokenStampLine` (QrId, BrokenAt) + giá trị `StampStatus.Broken`, service `ReportBrokenAsync` (mọi tem phải tồn tại, chặn tem đã ghi nhận lỗi ở phiếu khác, chặn tem Void, đánh dấu tem Broken), `BrokenStampController` + 3 view (Index/Create/Detail) + nav "Tem rách/vỡ" + seed phiếu mẫu. Cập nhật `VerifyAsync`/`ActivateAsync`/`SpinAsync` để loại tem Broken khỏi vòng đời (tra cứu công khai hiện "TEM ĐÃ RÁCH/VỠ"). Build Release 0 error; smoke test /BrokenStamp, /BrokenStamp/Create, /BrokenStamp/Detail, POST ghi nhận (thành công + chặn trùng + chặn mã sai) đều OK.
- 2026-09-23 · Port nghiệp vụ **xuất kho theo tem (Inv_VerifiedIDInOut / Inv_InvVerifiedID_OutGenInAndOut)** từ 2023.B.EQR — nghiệp vụ được tài liệu đánh dấu "★ phức tạp nhất ★": thêm entity `Shipment` (ShipmentNo, Status PENDING/SHIPPED, RefNoSys/RefNo/RefType, PlateNo/MoocNo/DriverName/DriverPhoneNo/TransportType/ReceivePlace, CustomerCode/Name/Address, ShippedAt/By) + `ShipmentLine` (QrId, ProductId) + 3 cột xuất trên `Stamp` (ShipmentId, ShippedAt, CustomerCode), service `CreateShipmentAsync` (mọi tem phải tồn tại, chặn tem Void/Broken, chặn tem đã xuất ở phiếu khác, chặn trùng mã phiếu) và `ShipShipmentAsync` (chỉ phiếu PENDING mới xuất, đánh dấu tem đã xuất + ghi khách hàng), `ShipmentController` + 3 view (Index/Create/Detail) + nav "Xuất kho theo tem" + seed phiếu mẫu PXK-SEED-001 (đã xuất 3 tem). Build Release 0 error. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **kích hoạt thông tin sản xuất (InvF_ProductionActive)** từ 2023.B.EQR: thêm entity `ProductionActive` (PaNo/IF_PANo, RefNo, Origin, ProductId, QtyPlan, ProductDate, ExpiryDate, ProductLifeId, ListSerialInManufacture/OutManufacture) + master data `ProductLife` (Mst_ProductLife: Code, Name, Type, Value, ValueByDay), service `CreateProductionActiveAsync` (RefNo bắt buộc + duy nhất, Origin bắt buộc, sản phẩm + hạn dùng phải tồn tại, dải serial vào SX bắt buộc, **ngày hết hạn = Ngày SX + ProductLifeValue − 1** đúng công thức EQR) và `DeleteProductionActiveAsync` (chỉ xóa trong 72h kể từ khi tạo), `ProductionActiveController` + 3 view (Index/Create/Detail) + nav "Kích hoạt SX" + seed 6 hạn dùng master + phiếu mẫu PA-SEED-001. Build Release 0 error. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **danh mục Nguồn gốc (Mst_NguonGoc)** từ 2023.B.EQR (theo `_docs/80-API-MST-NGUONGOC.md`): thêm entity `OriginCatalog` (Code/NguonGocCode, Name/NguonGocName, DisplayName, CertificateCode/No/Name, CertificateDateStart/End, Address, GlnCode, Remark, IsActive) — master data cho trường "Nguồn gốc" ở màn Kích hoạt SX, service `OriginsAsync` (lọc Code/Name/DisplayName phục vụ Auto Complete), `CreateOriginAsync` (Code bắt buộc + duy nhất, Name bắt buộc, DateStart ≤ DateEnd, **DisplayName tự dựng = "Code (CertCode CertNo)", rỗng chứng chỉ ⇒ = Code**), `UpdateOriginAsync` (bản ghi phải tồn tại, dựng lại DisplayName), `DeleteOriginAsync` (**chặn xóa nếu Code đang được dùng làm Origin của 1 phiếu kích hoạt SX**), `OriginController` + 3 view (Index có ô tìm kiếm/Create/Edit) + nav "Nguồn gốc" + seed 2 nguồn gốc mẫu (CP1/CP2). Build Release 0 error. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **truy xuất nguồn gốc theo chuẩn GS1 (module eTEM TruyXuat: Mst_CTE / Mst_KDE / CTE_KDE / Event_Event)** từ 2023.B.EQR (theo `_docs/20-DOMAIN-BUSINESS.md` §3 + hàm LIVE `WAS_Event_Event_Save_New20210922` trong `eTEMTruyXuat/Event.cs`): thêm entity `TraceEventType` (CTECode/Name/TplVECode/IsActive), `TraceKde` (KDECode/Name/DataType/IsKey=FlagKey/IsList=FlagList), `TraceEventTypeKde` (bảng nối CTE↔KDE), `TraceEvent` (EventNo/CTECode/UIStyleCode/GLNOrgCode/TplVECode/Status) + `TraceEventSpec` (KDECode/KDEValue), service `SaveTraceEventAsync` (CTECode + UIStyleCode bắt buộc; mọi KDE phải thuộc CTE — CTECode_KDECodeNotFound; tối đa 1 KDE danh sách — AllowOnlyOneListPerEvent; KDE khóa bắt buộc có giá trị + đủ số lượng khóa; sự kiện trùng bộ giá trị khóa ⇒ UPDATE thay vì tạo mới), `TraceController` + 4 view (Index/Catalog/Create/Detail, form KDE động theo CTE) + nav "Sự kiện truy xuất" + seed 3 CTE (HARVEST/PACKING/SHIPPING) + 6 KDE + gán CTE_KDE + sự kiện mẫu EV-SEED-001. Build Release 0 error, 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **phiếu xuất kho thành phẩm (InvF_InventoryOutFG)** từ 2023.B.EQR (worker LIVE `WAS_InvF_InventoryOutFG_Save`/`_Approve`, file `InventoryForm.cs`) — KHÁC với "xuất kho theo tem" (Inv_VerifiedIDInOut): phiếu này xuất theo mặt hàng (PartCode) + số lượng, không gắn từng con tem. Thêm entity `InventoryOutFG` (InvOutFGNo/IF_InvOutFGNo, Mst, FormOutType MAVACH/KHONGMAVACH, InvOutType, InvCode, PmType, InvFOutType OUTTHUONGMAI/OUTENDCUS, PlateNo/MoocNo/DriverName/DriverPhoneNo, AgentCode, CustomerName, Status PENDING/APPROVE/CANCEL, ApprovedAt/By) + `InventoryOutFGDtl` (PartCode, ProductId, Qty, SerialNo), service `CreateInventoryOutFGAsync` (mã phiếu bắt buộc + duy nhất, InvFOutType chỉ nhận OUTTHUONGMAI/OUTENDCUS, ≥1 dòng mặt hàng SL>0, mặt hàng phải tồn tại, phiếu mới PENDING), `ApproveInventoryOutFGAsync` (chỉ duyệt phiếu PENDING) và `DeleteInventoryOutFGAsync` (chỉ xóa phiếu PENDING), `InventoryOutController` + 3 view (Index/Create/Detail, form nhiều dòng động + serial) + nav "Xuất kho TP" + seed phiếu mẫu PXKTP-SEED-001 (đã duyệt, 2 dòng). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit 81b8798 đã push origin/main.
- 2026-09-23 · Port nghiệp vụ **hóa đơn điện tử (Invoice_Invoice)** từ 2023.B.EQR (bước (8) vòng đời tem; worker LIVE `WAS_Invoice_Invoice_Save_Root`/`_Approved`/`_AllocatedInv`/`_Issued`/`_Cancel`, file `Invoice.cs`): thêm entity `Invoice` (InvoiceCode, InvoiceNo, Status PENDING/APPROVED/ISSUED/CANCEL, TInvoiceCode + dải số InvoiceNoStart/End, RefNo/Mst/PaymentMethodCode, thông tin bên mua NNT, InvoiceDate, TotalValInvoice/Vat/Pmt, mốc duyệt/phát hành/hủy) + `InvoiceDtl` (PartCode, UnitName, Qty, UnitPrice, Amount), service `CreateInvoiceAsync` (InvoiceCode bắt buộc + duy nhất, ≥1 dòng mặt hàng SL>0, mặt hàng phải tồn tại, ngày HĐ không ở tương lai, thành tiền = SL×đơn giá, tổng TT = tiền hàng + VAT, phiếu mới PENDING), `ApproveInvoiceAsync` (chỉ PENDING), `IssueInvoiceAsync` (chỉ APPROVED; cấp số kế tiếp trong dải mẫu, hết dải ⇒ từ chối — đúng ràng buộc InvalidQtyIssueRemain của EQR) và `CancelInvoiceAsync` (không hủy trùng; HĐ đã phát hành phải ghi lý do), `InvoiceController` + 3 view (Index/Create/Detail, form nhiều dòng động) + nav "Hóa đơn điện tử" + seed hóa đơn mẫu HD-SEED-001 (đã phát hành, số 1, 2 dòng). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit 5cdc15a đã push origin/main.- 2026-09-23 · Port nghiệp vụ **ghép cặp tem (Map_StampPair)** từ 2023.B.EQR (worker LIVE `WAS_Map_StampPair_Add_New20220415` / `WAS_Map_StampPair_Get`, file `Template.cs`): thêm entity `StampPair` (MainQrId = tem chính/IDNo, SubQrId = tem phụ/BoxNo, Remark) với 2 unique index (OrgId+MainQrId, OrgId+SubQrId) đảm bảo mỗi tem chỉ thuộc tối đa 1 cặp, service `PairStampsAsync` (cả 2 tem phải tồn tại, tem chính ≠ tem phụ, chặn tem đã thuộc cặp khác ở vai trò chính hoặc phụ — đúng ràng buộc IDNoNotExistInInvGen/ExistIDNoInOtherBox của EQR), `StampPairController` + 3 view (Index/Create/Detail, Detail hiển thị 2 QR) + nav "Ghép cặp tem" + seed cặp tem mẫu (2 tem đầu lô). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit f27ad6e đã push origin/main.- 2026-09-23 · Port nghiệp vụ **danh mục địa điểm GS1 (Mst_GLN)** từ 2023.B.EQR (worker LIVE `WAS_Mst_GLN_Create_New20210408` / `_Update_New20210408` / `_Delete_New20210409` / `_Get`, file `eTEMTruyXuat/eTemNN.cs`): thêm entity `Gs1Location` (Code/GLNCode, Name/GLNName, GpsLat, GpsLong, OrgCode/OrgID, Remark, IsActive/FlagActive) với unique index (OrgId+Code), service `Gs1LocationsAsync` (lọc theo mã/tên/đơn vị sở hữu), `CreateGs1LocationAsync` (GLNCode bắt buộc + duy nhất — đúng ràng buộc Mst_GLN_CheckDB_MstGLNExist; GLNName bắt buộc; OrgID bắt buộc), `UpdateGs1LocationAsync` (bản ghi phải tồn tại — Mst_GLN_CheckDB_MstGLNNotFound; tên không rỗng; đơn vị sở hữu bắt buộc) và `DeleteGs1LocationAsync` (bản ghi phải tồn tại), `Gs1LocationController` + 3 view (Index/Create/Edit) + nav "Địa điểm GS1" + seed 2 địa điểm mẫu. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit ec22df7 đã push origin/main.- 2026-09-24 · Port nghiệp vụ **gom tem vào Block (Map_Block)** từ 2023.B.EQR (worker LIVE `WAS_Map_Block_Add_New20220701` → `Map_Block_AddX_New20230213`, file `InvGen.cs`; master `Mst_BlockType` + `Mst_BlockType_CheckDB` file `Master.cs`): thêm entity `Block` (BlockNo/BoxNo, BlockType, BlockLocalID, ShiftCode, LotCode, Qty=BlockSize, QtyVerified, Remark) + `BlockLine` (QrId) + master data `BlockType` (Code/BlockType, Name/BlockTypeName, BlockSize, IsActive/FlagActive), service `CreateBlockAsync` (BlockType phải tồn tại + còn dùng — đúng ràng buộc Mst_BlockType_CheckDB; ≥1 tem; mọi tem phải tồn tại; chặn tem đã thuộc block khác; chặn trùng mã block; Qty = BlockSize của loại block, QtyVerified = số tem thực tế), `BlockController` + 3 view (Index/Create/Detail) + nav "Block tem" + seed 6 loại block (MASTAMP/MABOX/MACAN/MAPACK/MAPALLET/MACONTAINER) + block mẫu BLK-SEED-001 (4 tem). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit f4c7e52 đã push origin/main.- 2026-09-23 · Port nghiệp vụ **kích hoạt bảo hành bằng PIN (WarrantyDateStartFromPIN_Activate)** từ 2023.B.EQR (worker LIVE `WAS_WarrantyDateStartFromPIN_Activate_New20250520` → `WarrantyDateStartFromPIN_ActivateX_New20220111`, file `Report.cs`): thêm entity `WarrantyActivation` (QrId, Pin, PhoneNoUser, IpAddress, MapLatitude/Longitude, WarrantyNo, WarrantyDateStart, IsFirstActivate, WarrantyCount) + 5 cột trên `Stamp` (WarrantyNo, WarrantyCount, WarrantyStartIp, WarrantyLat, WarrantyLong), service `ActivateWarrantyByPinAsync` (tem phải tồn tại; PIN phải khớp — so khớp PIN hoặc MD5(IDNo|PIN) đúng EQR; chặn tem Void/Broken; **lần đầu cấp WarrantyNo mới + ghi WarrantyDateStart**, **kích hoạt lại giữ nguyên WarrantyNo/WarrantyDateStart chỉ tăng WarrantyCount + cập nhật SĐT/IP/vị trí** — đúng 2 nhánh update của EQR; mỗi lượt ghi 1 bản ghi lịch sử), `WarrantyController` + 2 view (Index/Detail) + nav "Kích hoạt bảo hành" + form nhập PIN trên trang tra cứu công khai `/Verify` + seed 1 lượt kích hoạt mẫu (tem đầu lô). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit 9e51658 đã push origin/main.- 2026-09-23 · Port nghiệp vụ **tem trung tính / nghi vấn (Inv_InventoryNeutralID)** từ 2023.B.EQR (worker LIVE `WAS_Inv_InventoryNeutralID_InsertSuspectID`, file `zTemp.cs`): thêm entity `NeutralStamp` (QrId, FlagNeutral, OutCount, Note, ResolvedAt/By) với unique index (OrgId+QrId), service `DetectSuspectStampsAsync` (phát hiện tem bị **xuất kho nhiều lần** — xuất hiện ở > 1 dòng phiếu xuất, bỏ qua tem đã có bản ghi trung tính, ghi FlagNeutral=false) và `ResolveNeutralStampAsync` (xác nhận tem trung tính, chặn xác nhận trùng), `NeutralStampController` + 2 view (Index có nút "Rà soát tem nghi vấn" + lọc theo trạng thái, Detail có form xác nhận) + nav "Tem trung tính" + seed 1 bản ghi nghi vấn mẫu. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **yêu cầu xuất kho (InvF_ReqInvOut)** từ 2023.B.EQR (worker LIVE `WAS_InvF_ReqInvOut_Save` / `_Approve`, file `InventoryForm.cs`): thêm entity `ReqInvOut` (ReqInvOutNo, RefNo, InvCode, InvOutType, InvOutDate, TransportType, VehicleNumber, ReqStatus PENDING/APPROVE, CustomerCodeSys, ReceiveAddress, IVerifiedIDInOutNo, QRCodeOS, mốc duyệt) + `ReqInvOutDtl` (ProductCode, UnitCode, Qty, QtyId, FlagDiscount, Remark, QRCodeOS) với 2 unique index (OrgId+ReqInvOutNo, OrgId+RefNo), service `CreateReqInvOutAsync` (RefNo bắt buộc + duy nhất, ≥1 dòng mặt hàng SL>0, mặt hàng phải tồn tại, sửa chỉ khi PENDING — đúng ràng buộc InvalidReqStatus/InvalidRefNoExisted của EQR), `ApproveReqInvOutAsync` (chỉ PENDING; gắn IVerifiedIDInOutNo không được trùng phiếu khác — đúng IVerifiedIDInOutNoExistedReqInvOut), `UnApproveReqInvOutAsync` (chỉ APPROVE, xóa liên kết + mốc duyệt — nhánh FlagIsUnApprove) và `DeleteReqInvOutAsync` (chỉ PENDING — nhánh FlagIsDelete), `ReqInvOutController` + 3 view (Index/Create/Detail, form nhiều dòng động) + nav "Yêu cầu xuất kho" + seed phiếu mẫu YCXK-SEED-001 (đã duyệt, 2 dòng). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit b811f92.- 2026-09-23 · Port nghiệp vụ **vòng đời tem theo kỳ tháng (InvIVID_LifeCircleIDNoByPeriod)** từ 2023.B.EQR (worker LIVE `WAS_InvIVID_LifeCircleIDNoByPeriod_Add` → `InvIVID_LifeCircleIDNoByPeriod_AddX`, file `Template.cs`): thêm entity `StampLifecyclePeriod` (PeriodMonth chuẩn hoá về ngày đầu tháng, QtyVerifiedID/QtySales/QtyWarranty/QtySearch + Delta* so với kỳ trước, Remark), service `SnapshotLifecycleAsync` (kỳ chuẩn hoá yyyy-MM-01, mỗi kỳ chỉ chốt 1 lần — chặn trùng theo OrgId+PeriodMonth; đếm 4 chỉ số tem trong kỳ: đã ghép SP theo BoxedAt, đã bán theo SalesDTime, đã kích hoạt bảo hành theo ActivatedAt, đã tra cứu theo LastScanAt; **Delta = kỳ này − kỳ trước liền kề** đúng công thức EQR), `LifecyclePeriodsAsync`/`GetLifecyclePeriodAsync`, `LifecycleController` + 3 view (Index bảng số liệu + Δ màu, Create chọn kỳ tháng, Detail 4 thẻ thống kê) + nav "Vòng đời tem theo kỳ" + seed 1 kỳ mẫu (tháng trước). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **khôi phục hộp tem từ lịch sử (Map_IDInBox_RestoreBoxNo)** từ 2023.B.EQR (worker LIVE `WAS_Map_IDInBox_RestoreBoxNo` / `WAS_Map_IDInBoxHist_Get_ForRestoreBoxNo`, file `zTemp.1.cs`): thêm entity `BoxHistory` (BoxNo, FunctionName, RefType, CreateDTimeUTC, QtyIDNo, RestoredAt/By) + `BoxHistoryLine` (QrId) mô phỏng bảng `Map_IDInBoxHist`; `PackBoxAsync` nay tự ghi 1 bản ghi lịch sử mỗi lần đóng hộp; service `RestoreBoxAsync` (bản ghi phải tồn tại + chưa khôi phục, hộp đích phải tồn tại; tem đã xuất bán FlagSales hoặc đang trùng ở hộp khác ⇒ đưa vào bảng tem trung tính nghi vấn, còn lại gắn lại vào hộp — đúng ràng buộc EQR), `BoxHistoryController` + 2 view (Index lọc theo mã hộp / Detail hiển thị tem + nút khôi phục) + nav "Khôi phục hộp tem" + seed 1 lịch sử đóng hộp mẫu. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit aa4ef11 đã push origin/main.- 2026-09-23 · Port nghiệp vụ **hủy phiếu xuất kho theo tem (Inv_VerifiedIDInOut_Cancel)** từ 2023.B.EQR (worker LIVE `WAS_Inv_VerifiedIDInOut_Cancel_New20230316` → `Inv_VerifiedIDInOut_CancelX_New20230316`, file `Temp.cs`; controller `InvVerifiedIDInOutController.WA_Inv_VerifiedIDInOut_Cancel`): thêm 3 cột hủy trên `Shipment` (CancelledAt, CancelledBy, CancelReason), service `CancelShipmentAsync` (phiếu phải tồn tại, chặn hủy trùng, chỉ hủy được phiếu đã xuất SHIPPED, hủy xong Status=CANCEL + ghi mốc/người/lý do, **giải phóng tem** gỡ ShipmentId/ShippedAt/CustomerCode để tem trở về trạng thái chưa xuất — đúng tinh thần EQR "tem trong phiếu bị hủy trở về tem trắng"), action `ShipmentController.Cancel` + nút "Hủy phiếu" (kèm ô lý do + confirm) trong `Views/Shipment/Detail.cshtml` + hiển thị mốc/lý do hủy. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **phiên sản xuất (InvF_ProductionSession)** từ 2023.B.EQR (worker LIVE `WAS_InvF_ProductionSession_Add` → `InvF_ProductionSession_AddX`, file `zTemp.cs`): hoàn thiện nghiệp vụ đã có entity `ProductionSession`/`ProductionSessionLine` + khai báo interface nhưng CHƯA có implementation (baseline build đang lỗi 3× CS0535). Thêm service `ProductionSessionsAsync`/`GetProductionSessionAsync`/`CreateProductionSessionAsync` (IF_PSNo bắt buộc + duy nhất, sản phẩm phải tồn tại, ≥1 tem, mọi IDNo phải tồn tại trong kho sinh số, QtyVerified = số tem thực tế, **"rút ruột"**: tem đã thuộc phiên khác thì gỡ khỏi phiên cũ rồi chuyển sang phiên này — đúng giải thuật EQR), `ProductionSessionController` + 3 view (Index/Create/Detail) + nav "Phiên sản xuất" + seed phiên mẫu PS-SEED-001 (6 tem lô mẫu) + bổ sung 2 bảng vào migrate Postgres. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)- 2026-09-23 · Port nghiệp vụ **hoàn tác kích hoạt bán hàng (Inv_InventoryVerifiedID_FlagSalesBackStatus)** từ 2023.B.EQR (worker LIVE `WAS_Inv_InventoryVerifiedID_FlagSalesBackStatus` → `Inv_InventoryVerifiedID_FlagSalesBackStatusX`, file `zTemp.cs`): bổ sung bước đối ứng của "kích hoạt bán hàng" — đưa tem đã bán về trạng thái chưa bán. Thêm record `SalesRevertResult`, service `RevertSalesActivationAsync` (phiếu phải tồn tại; gỡ liên kết xuất bán trên từng tem: `FlagSales=false`, `SalesDTime=null`, `CustomerCode=null`, `SalesActivationId=null` — đúng các cột EQR set null + FlagSales='0'; xóa phiếu kích hoạt bán hàng, tương ứng EQR xóa bản ghi `Inv_VerifiedIDInOut`), action `SalesActivationController.Revert` (POST + antiforgery), 3 view `Views/SalesActivation/` (Index/Create/Detail — Detail có nút "Hoàn tác" kèm lý do) + nav "Kích hoạt bán hàng" + seed phiếu mẫu PXKHT-SEED-001 (đã bán 2 tem của lô). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit 9af4a65 đã push origin/main.- 2026-09-23 · Port nghiệp vụ **xuất kho theo hộp (Inv_InventoryVerifiedID_OutByBox)** từ 2023.B.EQR (worker LIVE `WAS_Inv_InventoryVerifiedID_OutByBox_New20220601` → `Inv_InventoryVerifiedID_OutByBoxX_New20220601`, file `zTemp.cs`) — KHÁC với "xuất kho theo tem" (Shipment): ở đây người dùng **quét mã**, mỗi mã là 1 tem lẻ (`StampType='ID'`) hoặc 1 **mã hộp** (`StampType='BOX'`), hệ thống tự **bung hộp ra toàn bộ tem con** rồi mới ghi phiếu. Thêm entity `BoxShipment` (BsNo, Status PENDING/SHIPPED/CANCEL, RefNoSys/RefNo/RefType, PlateNo/MoocNo/DriverName/DriverPhoneNo/TransportType/ReceivePlace, CustomerCode/Name/Address, ShippedAt/By, CancelledAt/By/Reason) + `BoxShipmentLine` (ScanCode = mã quét gốc, StampType = ID/BOX, QrId = tem thực tế đã xuất, ProductId), service `CreateBoxShipmentAsync` (StampType chỉ nhận ID/BOX — StampTypeInvalid; tem lẻ phải tồn tại — IDNoNotExistInInvGen; tem lẻ phải đã đóng vào hộp — IDNoNotMapBox; hộp phải tồn tại + có tem con; chặn tem Void/Broken; chặn tem đã xuất ở phiếu khác — cả phiếu theo tem lẫn theo hộp; chặn trùng mã phiếu), `ShipBoxShipmentAsync` (chỉ phiếu PENDING mới xuất, đánh dấu tem đã xuất + ghi khách hàng) và `CancelBoxShipmentAsync` (chỉ hủy phiếu SHIPPED, giải phóng tem về chưa xuất), `BoxShipmentController` + 3 view (Index/Create/Detail, Create nhận tiền tố `BOX:`/`ID:` để chỉ định loại quét) + nav "Xuất kho theo hộp" + seed phiếu mẫu PXKH-SEED-001 (quét 1 mã hộp BOX-SEED-001 + 1 tem lẻ). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)
- 2026-09-23 · Port nghiệp vụ **tem bí mật / serial ẩn (Inv_InventorySecret)** từ 2023.B.EQR (worker LIVE `WAS_Inv_InventorySecret_Get` / `WAS_Inv_InventorySecret_UpdateFlagUsed`, file `License.cs`): thêm entity `InventorySecret` (SerialNo, QrSerialNo, Mst, GenTimesNo, SecretNo, FlagMap, FlagUsed, UsedAt/By) với unique index (OrgId+SerialNo), service `CreateInventorySecretAsync` (SerialNo bắt buộc + duy nhất) và `MarkSecretsUsedAsync` (serial phải tồn tại — InvalidSerial; serial chưa dùng mới đánh dấu được — InvalidFlagUsed; serial phải thuộc đúng MST — InvalidMST; đúng ràng buộc EQR), `InventorySecretController` + 3 view (Index có lọc theo trạng thái + modal đánh dấu đã dùng, Create, Detail) + nav "Tem bí mật" + seed 3 serial mẫu gắn 3 tem đầu lô. Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit 1104eca đã push origin/main.
- 2026-09-23 · Port nghiệp vụ **nhập dãy serial người dùng (Inv_StampUser)** từ 2023.B.EQR (worker LIVE `WAS_Inv_StampUser_Add` → `Inv_StampUser_AddX`, file `DVP/ImportIDUser.cs`; controller `ImportIDUserController`): thêm entity `StampUser` (SuiNo/IF_SUINo, ImportDTime, Quantity) + `StampUserLine` (IdNoUser/IDNo_User, QrId = tem đã gắn, Remark) với unique index (OrgId+SuiNo, OrgId+IdNoUser), service `ImportStampUsersAsync` (IF_SUINo bắt buộc + duy nhất, ≥1 serial, serial không trùng serial đã nhập trước đó — StampUserIDNo_UserExist, số tem còn trống phải ≥ số serial nhập — InvalidValue, map theo thứ tự serial nhỏ nhất ↔ tem nhỏ nhất đúng EQR), `StampUserController` + 3 view (Index/Create/Detail) + nav "Nhập serial người dùng" + seed phiếu mẫu SUI-SEED-001 (gắn 2 serial vào 2 tem cuối lô). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.)
- 2026-09-23 · Port nghiệp vụ **kích hoạt tem trắng bởi Trạm bán hàng (Inv_InventoryVerifiedID_ActivateByTBH)** từ 2023.B.EQR (bước (6c) vòng đời tem; worker LIVE `WAS_Inv_InventoryVerifiedID_ActivateByTBH_New20210922`, file `zTemp.cs`; controller `InvInventoryVerifiedIDController`): thêm entity `TbhActivation` (TbhNo, RefNoSys tiền tố PXKHTT, RefType=INVOUT, QrId, Pin, ProductId, CustomerCode, AreaCode, ProofImagePath/Name) với unique index (OrgId+TbhNo), service `ActivateByTbhAsync` (IDNo bắt buộc + chuẩn hoá nhiễu bỏ space/'-'/tab/xuống dòng đúng EQR, tem phải tồn tại, chặn tem Void/Broken, chặn tem đã kích hoạt bán trước đó, tự sinh phiếu xuất nội bộ + đánh dấu tem FlagSales='1'+SalesDTime+CustomerCode), `TbhActivationController` + 3 view (Index/Create/Detail) + nav "Kích hoạt tem trắng (TBH)" + seed phiếu mẫu PXKHTT-SEED-001 (kích hoạt tem thứ 7 của lô). Build Release 0 error / 0 warning. (Chưa smoke-test runtime theo yêu cầu — chỉ build.) Commit b261422 (push origin/main bị từ chối do remote ref lock — đã commit local).
